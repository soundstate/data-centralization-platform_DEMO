# ============================================================================
# SHARED CONFIGURATION (All Environments)
# ============================================================================
# Application Information
APP_NAME=data-centralization-platform
APP_VERSION=1.0.0

# ============================================================================
# API RATE LIMITING CONFIGURATION
# ============================================================================
# Rate limits are defined in requests per minute for each external service
# These values are conservative to avoid rate limiting issues

# Music & Entertainment APIs
SPOTIFY_RATE_LIMIT=100
MUSICBRAINZ_RATE_LIMIT=1
TMDB_RATE_LIMIT=40

# Developer & Code APIs
GITHUB_RATE_LIMIT=5000

# Weather & Location APIs
OPENWEATHER_RATE_LIMIT=60

# Productivity & Collaboration APIs
NOTION_RATE_LIMIT=3

# Gaming APIs (Pok̩mon API has no explicit rate limit but should be conservative)
POKEMON_API_RATE_LIMIT=100

# AI & Language Model APIs
OPENAI_RATE_LIMIT=3500

# ============================================================================
# DEFAULT PAGINATION SETTINGS
# ============================================================================
# Standard pagination sizes for external APIs
SPOTIFY_DEFAULT_LIMIT=50
SPOTIFY_MAX_LIMIT=50
GITHUB_DEFAULT_LIMIT=30
GITHUB_MAX_LIMIT=100
TMDB_DEFAULT_LIMIT=20
NOTION_DEFAULT_LIMIT=100

# ============================================================================
# CORRELATION ANALYSIS DEFAULTS
# ============================================================================
# Universal thresholds for correlation analysis across all environments
DEFAULT_CORRELATION_THRESHOLD=0.5
DEFAULT_SIGNIFICANCE_LEVEL=0.05
DEFAULT_MINIMUM_SAMPLE_SIZE=30
DEFAULT_CONFIDENCE_INTERVAL=0.95

# ============================================================================
# API TIMEOUT CONFIGURATION
# ============================================================================
# Timeout values in seconds for external API calls
SPOTIFY_REQUEST_TIMEOUT=30
GITHUB_REQUEST_TIMEOUT=30
TMDB_REQUEST_TIMEOUT=30
OPENWEATHER_REQUEST_TIMEOUT=30
NOTION_REQUEST_TIMEOUT=30
MUSICBRAINZ_REQUEST_TIMEOUT=30
OPENAI_REQUEST_TIMEOUT=60
POKEMON_REQUEST_TIMEOUT=30
